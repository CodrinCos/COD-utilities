name: release-nuget
on:
  push:
    branches: 
    - main
    - alpha/*
  workflow_dispatch:
jobs: 
  release-to-nuget-repo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "👋 Hello World!"
      - name: Checkout
        uses: actions/checkout@v4.2.2
      - name: List files in repository
        run: echo "The repository ${{ github.repository }} contains the following files:" | tree
